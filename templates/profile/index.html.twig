{% extends 'base.html.twig' %}

{% block title %}Mon Profil - MangaThèque{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/pages/profile.css') }}">
{% endblock %}

{% block body %}
<div class="profile-container">
    <div class="profile-content">
        <!-- En-tête du profil -->
        <div class="profile-header">
            <div class="profile-avatar">
                👤
            </div>
            <h1 class="profile-name">{{ app.user.nom }}</h1>
            <p class="profile-email">{{ app.user.email }}</p>
        </div>

        <!-- Statistiques -->
        <div class="profile-stats">
            <div class="stat-card">
                <div class="stat-icon">📚</div>
                <div class="stat-number">{{ favorisCount }}</div>
                <div class="stat-label">Favoris</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📖</div>
                <div class="stat-number">{{ enCoursCount }}</div>
                <div class="stat-label">En cours</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">✅</div>
                <div class="stat-number">{{ termineeCount }}</div>
                <div class="stat-label">Terminées</div>
            </div>
        </div>

        <!-- Actions rapides -->
        <div class="profile-actions">
            <h2 class="actions-title">
                ⚡ Actions Rapides
            </h2>
            <div class="action-grid">
                <a href="{{ path('app_oeuvre_list') }}" class="action-btn">
                    <div class="action-icon">🔍</div>
                    <div class="action-content">
                        <h4>Explorer le Catalogue</h4>
                        <p>Découvrir de nouveaux mangas</p>
                    </div>
                </a>
                
                <a href="{{ path('app_favoris') }}" class="action-btn">
                    <div class="action-icon">❤️</div>
                    <div class="action-content">
                        <h4>Mes Favoris</h4>
                        <p>Voir ma collection de favoris</p>
                    </div>
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}