{% extends 'base.html.twig' %}

{% block title %}Import Massif - Administration{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('assets/css/vortex-theme.css') }}">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
{% endblock %}

{% block body %}
{% include 'partials/_navigation.html.twig' %}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4 class="mb-0">
                        <i class="fas fa-rocket"></i> Import Massif de Mangas Populaires
                    </h4>
                    <div>
                        <a href="{{ path('admin_import_mangadx') }}" class="btn btn-outline-primary me-2">
                            <i class="fas fa-download"></i> Import Simple
                        </a>
                        <a href="{{ path('admin_oeuvres') }}" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i> Retour aux œuvres
                        </a>
                    </div>
                </div>
                
                <div class="card-body">
                    {% for flash_type, flash_messages in app.flashes %}
                        {% for flash_message in flash_messages %}
                            <div class="alert alert-{{ flash_type == 'error' ? 'danger' : flash_type }} alert-dismissible fade show">
                                {{ flash_message }}
                                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            </div>
                        {% endfor %}
                    {% endfor %}

                    <div class="row">
                        <div class="col-md-8">
                            <div class="card border-warning">
                                <div class="card-header bg-warning text-dark">
                                    <h5 class="mb-0">⚡ Configuration de l'import massif</h5>
                                </div>
                                <div class="card-body">
                                    <form method="post" id="importForm">
                                        <div class="row">
                                            <div class="col-md-6">
                                                <div class="mb-3">
                                                    <label for="limit" class="form-label">
                                                        <strong>Nombre de mangas à importer</strong>
                                                    </label>
                                                    <select class="form-select" id="limit" name="limit">
                                                        <option value="10">10 mangas (Test)</option>
                                                        <option value="20" selected>20 mangas (Recommandé)</option>
                                                        <option value="50">50 mangas</option>
                                                        <option value="100">100 mangas (Attention !)</option>
                                                    </select>
                                                    <div class="form-text">
                                                        Plus il y a de mangas, plus l'import sera long
                                                    </div>
                                                </div>
                                            </div>
                                            
                                            <div class="col-md-6">
                                                <div class="mb-3">
                                                    <label for="rating" class="form-label">
                                                        <strong>Classification de contenu</strong>
                                                    </label>
                                                    <select class="form-select" id="rating" name="rating">
                                                        <option value="safe" selected>Safe (Tout public)</option>
                                                        <option value="suggestive">Suggestive (13+)</option>
                                                        <option value="erotica">Erotica (18+)</option>
                                                    </select>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="row">
                                            <div class="col-md-6">
                                                <div class="mb-3">
                                                    <label for="status" class="form-label">
                                                        <strong>Statut de publication</strong>
                                                    </label>
                                                    <select class="form-select" id="status" name="status">
                                                        <option value="">Tous les statuts</option>
                                                        <option value="ongoing">En cours</option>
                                                        <option value="completed">Terminé</option>
                                                        <option value="hiatus">En pause</option>
                                                        <option value="cancelled">Annulé</option>
                                                    </select>
                                                </div>
                                            </div>
                                            
                                            <div class="col-md-6">
                                                <div class="mb-3">
                                                    <label class="form-label">
                                                        <strong>Options</strong>
                                                    </label>
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="checkbox" id="dry_run" name="dry_run" value="1">
                                                        <label class="form-check-label" for="dry_run">
                                                            Mode simulation (aperçu sans import)
                                                        </label>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        
                                        <div class="d-grid gap-2">
                                            <button type="button" class="btn btn-info btn-lg" onclick="startSimulation()">
                                                <i class="fas fa-eye"></i> 🔍 Aperçu (Simulation)
                                            </button>
                                            <button type="button" class="btn btn-warning btn-lg" onclick="startImport()">
                                                <i class="fas fa-rocket"></i> 🚀 Lancer l'import massif
                                            </button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                            
                            <div class="card mt-4">
                                <div class="card-header">
                                    <h5 class="mb-0">🖥️ Commandes équivalentes</h5>
                                </div>
                                <div class="card-body">
                                    <h6>Mode simulation :</h6>
                                    <pre class="bg-dark text-light p-3 rounded"><code>php bin/console app:import-popular-mangas --limit=20 --dry-run</code></pre>
                                    
                                    <h6 class="mt-3">Import réel :</h6>
                                    <pre class="bg-dark text-light p-3 rounded"><code>php bin/console app:import-popular-mangas --limit=20 --rating=safe</code></pre>
                                    
                                    <h6 class="mt-3">Toutes les options :</h6>
                                    <pre class="bg-dark text-light p-3 rounded"><code>php bin/console app:import-popular-mangas --help</code></pre>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-4">
                            <div class="card border-info">
                                <div class="card-header bg-info text-white">
                                    <h5 class="mb-0">ℹ️ Comment ça fonctionne ?</h5>
                                </div>
                                <div class="card-body">
                                    <ol class="list-unstyled">
                                        <li class="mb-3">
                                            <span class="badge bg-primary me-2">1</span>
                                            <strong>Recherche automatique</strong><br>
                                            <small class="text-muted">Récupère les mangas les plus populaires sur MangaDx</small>
                                        </li>
                                        <li class="mb-3">
                                            <span class="badge bg-primary me-2">2</span>
                                            <strong>Filtrage intelligent</strong><br>
                                            <small class="text-muted">Selon vos critères (nombre, classification, statut)</small>
                                        </li>
                                        <li class="mb-3">
                                            <span class="badge bg-primary me-2">3</span>
                                            <strong>Import automatique</strong><br>
                                            <small class="text-muted">Chaque manga et ses chapitres sont importés</small>
                                        </li>
                                        <li>
                                            <span class="badge bg-success me-2">✓</span>
                                            <strong>Base de données remplie</strong><br>
                                            <small class="text-muted">Prêt pour vos utilisateurs !</small>
                                        </li>
                                    </ol>
                                </div>
                            </div>

                            <div class="card border-danger mt-3">
                                <div class="card-header bg-danger text-white">
                                    <h6 class="mb-0">⚠️ Attention</h6>
                                </div>
                                <div class="card-body">
                                    <ul class="small mb-0">
                                        <li><strong>Respectez l'API :</strong> Des pauses sont automatiquement ajoutées</li>
                                        <li><strong>Durée :</strong> 100 mangas ≈ 10-15 minutes</li>
                                        <li><strong>Espace disque :</strong> Chaque manga prend ~500KB-2MB</li>
                                        <li><strong>Simulation :</strong> Testez d'abord avec le mode aperçu</li>
                                    </ul>
                                </div>
                            </div>
                            
                            <div class="card border-success mt-3">
                                <div class="card-header bg-success text-white">
                                    <h6 class="mb-0">📊 Estimation</h6>
                                </div>
                                <div class="card-body">
                                    <div class="text-center">
                                        <div class="mb-2">
                                            <strong id="estimatedTime">~2-5 minutes</strong><br>
                                            <small>Temps estimé</small>
                                        </div>
                                        <div class="mb-2">
                                            <strong id="estimatedSize">~10-40 MB</strong><br>
                                            <small>Espace disque</small>
                                        </div>
                                        <div>
                                            <strong id="estimatedChapters">~200-500</strong><br>
                                            <small>Chapitres total</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.card {
    transition: all 0.3s ease;
}

.card:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.btn {
    transition: all 0.3s ease;
}

.btn:hover {
    transform: translateY(-1px);
}

pre code {
    font-size: 0.9em;
}

.badge {
    font-size: 0.8em;
}

.form-control:focus, .form-select:focus {
    border-color: #007bff;
    box-shadow: 0 0 0 0.2rem rgba(0,123,255,.25);
}

.loading {
    opacity: 0.6;
    pointer-events: none;
}
</style>

<script>
function updateEstimates() {
    const limit = parseInt(document.getElementById('limit').value);
    
    // Estimations basées sur les données moyennes MangaDx
    const timePerManga = 3; // secondes
    const sizePerManga = 0.5; // MB
    const chaptersPerManga = 10; // moyenne
    
    const totalTime = Math.ceil((limit * timePerManga) / 60);
    const totalSize = Math.ceil(limit * sizePerManga);
    const totalChapters = limit * chaptersPerManga;
    
    document.getElementById('estimatedTime').textContent = `~${totalTime} minute${totalTime > 1 ? 's' : ''}`;
    document.getElementById('estimatedSize').textContent = `~${totalSize} MB`;
    document.getElementById('estimatedChapters').textContent = `~${totalChapters}`;
}

function startSimulation() {
    document.getElementById('dry_run').checked = true;
    document.getElementById('importForm').submit();
}

function startImport() {
    if (confirm('Êtes-vous sûr de vouloir lancer l\'import massif ? Cette opération peut prendre plusieurs minutes.')) {
        document.getElementById('dry_run').checked = false;
        document.getElementById('importForm').submit();
    }
}

// Mettre à jour les estimations au changement
document.getElementById('limit').addEventListener('change', updateEstimates);

// Initialiser les estimations
updateEstimates();
</script>
{% endblock %}

{% block javascripts %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %} 