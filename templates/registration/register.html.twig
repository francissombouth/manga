{% extends 'base.html.twig' %}

{% block title %}Inscription - MangaThèque{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/pages/auth.css') }}">
{% endblock %}

{% block body %}
<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <div class="auth-logo">🎌</div>
            <h1 class="auth-title">Rejoignez MangaThèque</h1>
            <p class="auth-subtitle">Créez votre compte pour découvrir des milliers de mangas</p>
        </div>

        {{ form_start(registrationForm, {'attr': {'class': 'auth-form'}}) }}
            <div class="form-group">
                {{ form_label(registrationForm.nom, 'Nom d\'affichage', {'attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.nom, {'attr': {'class': 'form-input', 'placeholder': 'Votre nom d\'affichage'}}) }}
                {{ form_errors(registrationForm.nom, {'attr': {'class': 'form-error'}}) }}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.email, 'Adresse email', {'attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.email, {'attr': {'class': 'form-input', 'placeholder': 'votre@email.com'}}) }}
                {{ form_errors(registrationForm.email, {'attr': {'class': 'form-error'}}) }}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.plainPassword, 'Mot de passe', {'attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'form-input', 'placeholder': 'Mot de passe sécurisé'}}) }}
                {{ form_errors(registrationForm.plainPassword, {'attr': {'class': 'form-error'}}) }}
            </div>

            <div class="checkbox-group">
                {{ form_widget(registrationForm.agreeTerms) }}
                {{ form_label(registrationForm.agreeTerms, 'J\'accepte les conditions d\'utilisation') }}
            </div>
            {{ form_errors(registrationForm.agreeTerms, {'attr': {'class': 'form-error'}}) }}

            <button type="submit" class="auth-submit">
                ✨ Créer mon compte
            </button>
        {{ form_end(registrationForm) }}

        <div class="auth-divider">
            <span>ou</span>
        </div>

        <div class="auth-link">
            <p>Déjà un compte ? <a href="{{ path('app_login') }}">Se connecter</a></p>
        </div>
    </div>
</div>
{% endblock %}