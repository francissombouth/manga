<div class="header">
    <div class="container">
        <nav class="nav">
            <a href="{{ path('mangadx_home') }}" class="logo">🎌 MangaThèque</a>
            <ul class="nav-links">
                <li><a href="{{ path('mangadx_home') }}">📚 Catalogue</a></li>
                <li><a href="{{ path('mangadx_search') }}">🔍 Recherche</a></li>
                <li><a href="{{ path('app_oeuvre_list') }}">⭐ Mes Favoris</a></li>
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('admin_dashboard') }}">⚙️ Administration</a></li>
                    {% endif %}
                    <li><a href="{{ path('app_profile') }}">👤 {{ app.user.nom }}</a></li>
                    <li><a href="{{ path('app_logout') }}">👋 Déconnexion</a></li>
                {% else %}
                    <li><a href="{{ path('app_login') }}">🔐 Connexion</a></li>
                    <li><a href="{{ path('app_register') }}">✨ Inscription</a></li>
                {% endif %}
            </ul>
        </nav>
    </div>
</div> 