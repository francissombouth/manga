# Enable stateless CSRF protection for forms and logins/logouts
framework:
    csrf_protection:
        enabled: true
        stateless_token_ids:
            - submit
            - logout

    form:
        csrf_protection:
            enabled: true
            field_name: '_token'
